#ifndef TREE_H
#define TREE_H

#include "game.h"
#include <vector>
/*
struct Node
{
  // stuff for move
  int index;//index of the Wriggler
  Parts part; // which part of w it is
  int dcol;  //dest col
  int drow;  //dest row
  
  //stuff for tree
  Node * p; //parent
  int depth;
  Node<vector> * children;
  //copy of game class
  void addChild(Node chlid);
  
};

struct Node
{
  // pointer to parent
  Node * p;
  
  // the move that it does
  // pointers to child nodes( array?)
  //    the moves that it can gen each one is a child
  //
  int depth
  void setUp(Node * parent, int depth, 
};

class Tree
{
  private:
    int size;
    Node root;



};
*/
#endif
